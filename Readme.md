# Install  
Use "npm install gulp gulp-sass --save-dev" and next to "gulp" .
# FixedHeader
Example:

```js
$(document).ready( function () {
    $('#myTable').DataTable( {
    	fixedHeader: true
    } );
} );
```


